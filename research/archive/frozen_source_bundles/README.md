# Frozen source bundles

These byte-preserved source bundles support historical pre-freeze hash checks after the active
Python orchestration was consolidated under `pyresearch/`. They are provenance artifacts, not
active import paths.
