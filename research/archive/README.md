# Research archive index

Older research evidence is retained for reproducibility. The current portfolio entry points are
[`../project_final/`](../project_final/) and [`../native_executable_pnl/`](../native_executable_pnl/).

Legacy aggregate-trade backtest results are retained in
[`legacy_aggregate_trade_baseline/`](legacy_aggregate_trade_baseline/). Detailed historical
write-ups live in [`docs/`](docs/), while the byte-preserved source receipt required by a frozen
passive-fill audit is in [`frozen_source_bundles/`](frozen_source_bundles/). Active retained
Python research code is consolidated under [`../../pyresearch/`](../../pyresearch/).
